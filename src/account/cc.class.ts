import { Account } from "./account.class";

export class CC extends Account {

    // constructor(number: number, agency: string, balance: number = 0) {
    //     super(this.number, this.agency, this.balance)
    // }
}